# Production environment — loaded automatically by Vite when `mode=production`
# (which `npm run build` uses). Committed to git so a fresh clone + build
# produces a frontend pointing at the real backend, not localhost.
# Override locally with a `.env` or `.env.local` file (gitignored).

VITE_API_BASE_URL=https://zhoujun123.pythonanywhere.com/api/