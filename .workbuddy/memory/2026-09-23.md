# 2026-09-23

## LC 102 二叉树的层序遍历 —— 算法系列第十篇，端到端发布完成

按 `algorithm-article-publish` skill 全流程发布 LC 102（含 103 锯齿 / 199 右视图两个变体）。

### 产出
- `article/lc-102-binary-tree-level-order-traversal.md`（11 节，20 分钟阅读）
- `frontend/src/viz/levelOrderSteps.js`（状态机，`mode: plain | zigzag`，15 帧）
- `frontend/src/viz/levelOrder.js`（SVG 渲染，树 + 队列条 + 结果区三段式）
- `frontend/src/viz/__tests__/levelOrderSteps.test.js`（41 例）
- `tools/make_lc102_cover.py` / `tools/publish_lc102_article.py`
- 封面双份：`frontend/public/photos/lc-102-cover.png` + `photos/lc-102-cover.png`
- `MarkdownView.vue` 注册 `'algo-viz--lc102': mountLevelOrder`
- 线上文章 id=62，标签新增「二叉树」(id=138)

### 技术要点
- **`size = len(queue)` 快照是层序全部难点**：内层一边出队一边入队，实时长度会串层。
- **满二叉树槽位布局法**：`slotUnit(d) = LEAF_STEP * 2^(maxDepth-d)`，路径当二进制读列号。
  初版指数写反（根层最窄）导致兄弟节点重叠。
- **锯齿用头插代替事后 reverse**；队列左右入队顺序永远不变，只反输出那一头。
- **199 右视图与锯齿冲突**：锯齿下 `levelValues` 末位变成左边那个，右视图必须另算。
- 全量单测 262 → 303 通过。

### 修复的坑
- 单测口径：`current` 统计要收敛到 `phase === 'consume'`，`enqueue`/`close` 帧会沿用 `current`。
- 渲染层三处布局 bug：`head` 标记与 `size` 括号叠字、括号压住 `res` 首行、
  `queue` 标签因 `text-anchor: end` + `x=30` 被左边缘裁掉。改为显式行高常量表 + `text-anchor: start`。

### 部署链路（重要，已核实）
- **GitHub Pages 从 `master` 部署**，`deploy.sh` 的 `git push origin master` 是对的。
- ⚠️ **`git log origin/master` 会显示 2018 年的陈旧提交**（历史 rebase 残留引用），
  据此会误判成「远端 master 已废弃」。判断部署状态只信
  `api.github.com/.../deployments` 或直接抓线上 URL。
- **第 2 次 deploy.sh 提交成功但推送没发出**，导致线上 `articles.json` 仍是 53 条、
  页面 404。手动 `git push origin master` 补推 `b3e12a7` 后恢复正常（54 条，页面 200）。
- 教训：**deploy.sh 是后台长任务，必须确认日志末尾出现 `master -> master`**；
  更稳的探针是部署后立刻抓 `articles.json` 看总数是否 +1。

### 线上验证
文章页 200（60977 bytes），封面 200（41264 bytes），`articles.json` 54 条，
首页已列出新文章，`ArticleDetail-C8krJNrm.js` 含 `mountLevelOrder`。

---

## LC 236 二叉树的最近公共祖先 —— 算法系列第十一篇，端到端发布完成

同一套 skill 流程（正文 md → viz 状态机 + SVG + vitest → 注册占位符 → 视觉验证
→ PIL 封面双份 → 四守卫发布脚本 + dry-run → 部署 → API 发布 → 二次构建 → 提交推送 → 线上验证）。

### 产出
- `article/lc-236-lowest-common-ancestor-of-a-binary-tree.md`（11 节，31 分钟阅读，含 23 帧推演表）
- `frontend/src/viz/lcaSteps.js`（状态机，显式栈 + stage 状态机 + deliverRet 回传，23 帧）
- `frontend/src/viz/lca.js`（SVG 渲染，树 + 递归栈 + 返回值传送带三段式）
- `frontend/src/viz/__tests__/lcaSteps.test.js`（51 例）
- `tools/make_lc236_cover.py` / `tools/publish_lc236_article.py`
- 封面双份：`frontend/public/photos/lc-236-cover.png` + `photos/lc-236-cover.png`（53868 bytes）
- `MarkdownView.vue` 注册 `'algo-viz--lc236': mountLca`

### 技术要点
- **三种返回值含义**：`null`（子树里一个都没有）/ `p` 或 `q` 本身（**只是信使，不是答案**）
  / LCA 自己（答案，继续往上冒）。
- **「一个节点也可以是它自己的祖先」自动处理**：靠 `if not root or root is p or root is q: return root`
  这一行「命中即返回，不再下探子树」。**不需要为「p 是 q 的祖先」单独写分支。**
- **分叉点判据**：`if left and right: return root` —— 左右都非空的那个节点是 LCA，**全树唯一**。
- **显式栈模拟递归**：栈帧 `{ id, stage, from }`，stage 有 `enter / left / right / decide`。
- **`deliver` 必须在 `stack.pop()` 之前调用**（初版踩坑：返回到爷爷身上而非父节点，
  主例只出 8 帧、子树完全没展开）。`frame.from`（'L'|'R'|null）标明是父节点的哪一侧孩子。
- 演示默认例用 **p=6, q=4**（不是官方第一例 p=5,q=1）—— 官方例只有 8 帧，看不出后序回溯。
- **`answer` 的裁定要收尾统一做**：分叉点情形在 decide 阶段就能写 answer；但**祖先情形命中时
  根本走不到 decide**（命中即返回），只能靠「根最终冒出来的返回值」来定。
  单测就是这么抓出 bug 的（13 例挂掉，全是祖先场景 answer=null）。
- **`alreadyAnswer` 标注**：`ret === answer` 时把 `returnSource` 标成 `'lca'`，
  否则答案在往上冒的路上会被误标成 `'p'`/`'q'`。

### 修复的坑
- **单测口径**：不变量用例必须给「树里真实存在的 p/q」—— 默认 6/4 在小树上不存在，
  `pId`/`qId` 是 `undefined`，递归一个都命中不了（是测试构造问题，不是状态机问题）。
- **「左右都非空只出现一次」的判据要收敛**：不能只看 `phase==='decide' && returnSource==='lca'`
  —— 答案向上冒时途经的祖先也会标 `'lca'`。正确判据是查 `leftOf`/`rightOf` 两边都非空。
- **返回值标签布局**：初版挂左上角，会和「左孩子的标签」叠（父在左上、子在左下，都往左上飘）。
  改成**贴节点正上方居中**，落在两层之间的空当里。
- **栈行宽度**：`STACK_ROW_W` 152 → 214（`depth N · dfs(x) · 刚进入` 会溢出裁字），
  `STACK_ROW_GAP` 5 → 10（相邻行贴太近）。
- **`paint(_index, step)` 签名**：`onRender(index, steps[index])` 第一参是 index 不是 step。
- **收尾照抄 `levelOrder.js`**：`player.jumpTo(Math.trunc(options.initialStep) || 0)` +
  `reduced` motion 判定 + IntersectionObserver 懒播放 + destroy 里 `observer.disconnect()`。

### 状态机验证结果（23 帧 trace 逐行核对过）
p=6/q=4→5（23帧）、p=5/q=1→3（8帧）、p=5/q=4→5（13帧，答案冒泡）、p=6/q=8→3（23帧）、
p=7/q=4→2（23帧）、p=0/q=8→1（23帧）、p=7/q=7→7、p=3/q=8→3、`[1,2]`p=1/q=2→1、
`[1,2,null,3]`p=2/q=3→2、空树 1 帧。

